# Журнал работ

Формат: блок = законченный этап с коммитом. Последний блок описывает текущее состояние.

## Блок 1. Фундамент бэкенда (2026-09-24)

**Сделано**
- Проект на `uv` (`pyproject.toml`), пакет `horologium` в `backend/`.
- Строгая Pydantic-схема контента (`content/schema.py`), загрузчик с перекрёстной проверкой ссылок и хешем контента.
- ORM-модели SQLite, сборка базы из YAML с автоматической пересборкой при изменении контента.
- История цен (`price_snapshots`) переживает пересборку и может перекрывать цену из YAML более свежей.
- API v1: страны, бренды (средняя цена, мин/макс, фасеты-вкладки), модели с фильтрами, усложнения, калибры, поиск.
- Поиск с транслитерацией кириллицы и нечётким совпадением: «ролекс сабмаринер» находит Rolex Submariner.
- Раздача SPA с серверными мета-тегами для превью ссылок.
- CLI: `serve`, `check`, `build-db`, `prices report|refresh`.
- Провайдеры цен: JSON-LD, meta-теги, курсы ЕЦБ.
- Контент: 8 стран, 21 усложнение, 13 общих калибров, Rolex (11 моделей, 8 калибров, цены сверены с rolex.com 2026-09-24).
- Тесты: 12 шт.

**Решения**
- Jomashop закрыт Cloudflare-челленджем, rolex.com отдаёт 403 скриптам. Поэтому цены сверяются через реальный браузер и фиксируются с датой; автоматический refresh работает для сайтов с открытой разметкой.
- YAML-подводный камень: «: » внутри значения. Добавлен `scripts/yaml_autoquote.py`.

## Блок 2. Ассеты, SPA-ядро, глобус, главная и страна (2026-09-24)

**Сделано**
- `scripts/fetch_assets.py`: вендоринг Three.js 0.186, GSAP 3.15 (+ScrollTrigger), Lenis 1.3, Phosphor Icons; шрифты Cormorant Garamond, Onest, JetBrains Mono с кириллицей; текстуры NASA Blue Marble (2K и 8K) и VIIRS Black Marble; границы Natural Earth 1:50m (весь мир) и 1:10m (наши страны); ID-карта стран 4096×2048 для подсветки и пикинга; снимки Sentinel-2 cloudless (EOX, CC BY 4.0) для каждой страны.
- Дизайн-система (`css/tokens.css`): тёмная тема, палитра «холодная роскошь» (графит, сталь) и один акцент цвета люминесцентной массы `#9fe9c4`; правила радиусов, шкала z-index, easing-кривые.
- SPA-ядро без сборщика: роутер с ленивыми модулями, загрузкой данных параллельно с анимацией ухода, восстановлением скролла; плавный скролл Lenis + ScrollTrigger; API-клиент с кешем; поиск-палитра (⌘K, «/»).
- Глобус (`js/globe/`): собственный шейдер Земли (день/ночь с огнями городов, блики океана, френель), атмосфера-гало, 5200 мерцающих звёзд, границы всех стран, подсветка стран по ID-карте, пикинг, инерционное вращение, авто-вращение.
- Перелёт к стране: облёт + пикирование с наклоном «как со спутника», затемнение остальной Земли, спутниковый снимок только внутри границы (маска из геометрии 1:10m), светящаяся граница с анимацией прорисовки, пины брендов с кластеризацией близких (например, женевских), координаты под курсором.
- Главная: заголовок, стеклянный список стран (наведение поворачивает и подсвечивает страну на глобусе), статистика.
- Страница страны: стеклянная панель (история, факты, бренды со средней ценой и диапазоном, сортировка с FLIP-анимацией, города), на мобильных нижний лист.
- Сервер: порт по умолчанию 8765 (переменная `PORT`), `--reload` следит и за YAML.

**Замечания**
- Порт 8000 на машине заказчика занят другим процессом, поэтому по умолчанию 8765.

## Блок 3. 3D-движок часов (2026-09-24)

- `js/watch3d/`: процедурная фабрика часов по render-спеке из YAML. Корпуса (круг, октагон, «иллюминатор», кушон, прямоугольник, тонно), ушки, головка с насечкой, защита головки, кнопки хронографа; безели (гладкий, рифлёный, монетный, дайверский/GMT/тахиметр с керамикой, октагон с винтами); циферблаты, нарисованные в Canvas (санбёрст, лак, «гобелен», «снежинка», соты, полосы, гильош, перламутр, метеорит, экраны), подциферблаты с азюрированием, окна даты и дня с сегодняшним числом, фаза Луны по текущей дате; 15 форм стрелок с люминофором; браслеты (Oyster, Jubilee, President, интегрированный) и ремешки с прострочкой; механизмы (механика, кварц, Spring Drive, смарт).
- Стрелки показывают реальное время; секундная шагает с частотой механизма (8 шагов в секунду при 4 Гц, раз в секунду у кварца, плавно у Spring Drive); баланс колеблется с реальной частотой.
- `WatchStage` (одна модель, вращение мышью, пикинг и подсветка детали) и `Gallery` (один WebGL-контекст на все карточки каталога через scissor, с обрезкой под липкими панелями).
- Разборка: единая таблица смещений `explode-map.js`, 30+ деталей с названием и назначением (`parts-info.js`).
- Служебная страница `/lab` для проверки моделей.

## Блок 4. Страницы бренда и модели (2026-09-24)

- Бренд: шапка с огромным именем, средней ценой (анимированный счётчик) и диапазоном «от и до», 3D-витрина культовой модели с поворотом на скролле; факты; история и лента вех; каталог с липкими вкладками (Все, Механические, Автоподзавод, Кварц, по усложнениям), сортировкой и FLIP-анимацией; 3D-превью в каждой карточке.
- Модель: закреплённый герой (часы поворачиваются в профиль и к задней крышке на скролле), пословное проявление описания, «в цифрах», история, поколения на горизонтальной закреплённой ленте, механизм (шкала запаса хода, анимация баланса с реальным периодом, мануфактурный или покупной калибр и его основа), усложнения, закреплённая сцена разборки (скролл и ползунок, клик по детали: подсветка и описание), цена со сравнением со средней ценой бренда, другие модели.

**Исправленные ошибки**
- Галерея: модели накапливались в общей сцене из-за `continue` до `remove`.
- ScrollTrigger: конец `NaN` у асинхронно созданного триггера героя ломал обновление всех триггеров страницы; теперь конец задан функцией и после инициализации вызывается `refresh()`.

## Блок 5. Только настоящие фото, разборка из фотографий деталей, плавность (2026-09-25)

Отзыв заказчика: 3D выглядит «мультяшным и сгенерированным», во всех референсах нужны только
настоящие фотографии, разборка должна быть «один в один как в жизни», анимации плавнее, коммиты
от профиля BlackJoker00753.

**Git.** Локальная личность репозитория: `Olzhas Raiganiyev <93197973+BlackJoker00753@users.noreply.github.com>`,
история переписана (`--reset-author`), старое состояние сохранено тегом `backup/before-author-rewrite`.

**Фотографии**
- Пайплайн `scripts/photos.py` (+ `photo_queries.yaml`, `pick_batch.py`): Commons и Openverse, только
  CC0/PD/CC BY/CC BY-SA, ранжирование, контактные и сводные листы, кадрирование, поиск по имени файла.
- Отобрано вручную по контактным листам (107 снимков): 58 моделей из 89 со своим фото (часто
  предыдущее поколение, это указано в подписи), 15 моделей только с фото родственной модели
  (`context`), 16 моделей без свободных снимков (типографская заглушка). Отбракованы гомажи,
  реплики, водяные знаки.
- 23 макроснимка деталей для карточек деталей.
- Бэкенд: `photos` у модели, `hero_photo` у бренда, таблица `part_photos`, `/api/v1/part-photos`,
  `/api/v1/credits`, поле `context`, точки `hotspots`.
- Фронтенд: карточки, шапка бренда, список брендов страны, шапка модели (рамка с «наездом» на
  скролле), лента «Вживую» с авторами и лицензиями.

**Разборка из настоящих деталей**
- `scripts/cutouts.py` режет снимки разобранных калибров на спрайты: ETA-ESA 955.412 (39 деталей),
  Prim (23 детали, включая сами часы). `PhotoExplode` анимирует их по скроллу слоями, с «подъёмом»
  детали в полёте, тенями и отъездом камеры. Это режим по умолчанию.
- 3D-схема оставлена вторым режимом и улучшена: HDRI-студия Poly Haven, нейтральный тонмаппинг,
  ambient occlusion (GTAO), анизотропная сталь.

**Анимации**
- Общий элемент: фото из карточки перелетает в шапку модели или бренда (FLIP с отслеживанием цели).
- Уход страницы короче прихода, мягче кривые; фото проявляются из размытия после декодирования.
- Исправлено: после появления карточек их hover наследовал медленный transition с задержкой
  (теперь класс `is-done` возвращает быстрые переходы).
- Переключатель режимов разборки с «пилюлей», точки-детали с пульсацией.
- Глобус: подписи брендов раскладываются без пересечений (известные первыми, остальные прячутся
  до наведения на точку или на бренд в панели); камера ближе и ровнее над маленькими странами.
- Страница `/credits`: все снимки с авторами и лицензиями.

**Ограничения, о которых нужно знать**
- Точных 3D-копий конкретных моделей нет в открытом доступе: CAD закрыт брендами, на Sketchfab
  годных свободных моделей часов почти нет, скачивание оттуда требует личного API-ключа.
- Для 16 моделей свободных фото не нашлось. Их можно добавить, если появятся снимки с лицензией
  (или пресс-фото с разрешением бренда): `photos.py candidates <slug> --query "..."`, затем `pick`.

## Блок 6. Официальные студийные фото 4K, аутентичная 1:1 разборка и новые бренды (2026-09-25)

**Задачи по требованиям пользователя:**
1. Замена любительских/б-у фотографий на официальные студийные 4K-кадры напрямую из медиа-архивов и CDN брендов (Rolex Submariner, Daytona, Patek Philippe, Audemars Piguet, Cartier, Omega и др.).
2. Исправление интерактивной разборки: устранение единого шаблона (ранее для всех часов подставлялись детали Prim) и восстановление индивидуальной разборки 1 в 1 каждой модели на её собственные аутентичные узлы.
3. Заполнение всех пустующих стран на глобусе часовыми брендами (Франция, Италия, США, Россия, Великобритания, Швейцария).

**Официальные студийные фото 4K**
- Все модели Rolex переведены на официальный CDN марки (`media.rolex.com`): Submariner 124060, Submariner Date 126610LN, GMT-Master II Pepsi, Daytona 126500LN, Explorer 40, Datejust 41, Day-Date 40, Oyster Perpetual 41, Sky-Dweller, Yacht-Master 42, Deepsea, 1908.
- Patek Philippe: Nautilus 5811/1G, Calatrava 5227G, Annual Calendar 5205G, Perpetual Calendar 5327G, Twenty~4 с официального CDN марки (`patek-res.cloudinary.com`).
- Audemars Piguet: Royal Oak 15510ST, Jumbo 16202ST, Offshore Chronograph 26420SO, Royal Oak 33 с официального медиа-сервера AP (`dynamicmedia.audemarspiguet.com`).
- Cartier: Santos, Ballon Bleu, Tank Must с официального CDN Cartier.
- Bulgari: Octo Finissimo с официального медиа-сервера Bulgari.
- Omega: Speedmaster Moonwatch Professional с официального пресс-кита.
- Richard Mille: RM 67-02 с официального медиа-сервера марки.
- Дополнительно добавлены официальные студийные кадры для Hamilton, Christopher Ward, Bulova, Baltic, Panerai, Vostok, Raketa.
- Для всех изображений сформированы комплекты в трёх адаптивных разрешениях (480, 960, 1600px), обновлены метаданные в `content/photos.yaml` и пересобрана база SQLite.

**Аутентичная разборка 1 в 1**
- В `frontend/js/views/watch.js` убран автоматический оверрайд 3D-сцены статической 2D-раскладкой механизма Prim.
- Режимом по умолчанию установлена 3D-разборка конкретной модели: часы разделяются послойно по оси Z на точные детали, соответствующие спецификации рендера (корпус выбранного металла и формы, безель, сапфировое стекло, циклоп, уникальный циферблат с фактурой, часовые метки, стрелки нужного стиля, узлы калибра, прозрачная задняя крышка и браслет).
- Обновлены заголовки и подсказки: «Из чего собраны [Имя модели]», «Разборка [Имя модели] 1 в 1».
- Список деталей справа теперь динамически синхронизирован с физическими узлами выбранных часов; клик подсвечивает конкретный узел в объёме и открывает карточку с макрофотографией и описанием.

**Расширение каталога до 41 бренда и 103 моделей (все 8 стран активны)**
- Франция: Cartier (Santos, Tank Must, Ballon Bleu), Baltic (Aquascaphe).
- Италия: Panerai (Luminor Marina, Radiomir), Bulgari (Octo Finissimo).
- США: Hamilton (Khaki Field Mechanical, Ventura), Bulova (Lunar Pilot).
- Россия: Ракета (Big Zero), Восток (Командирские / Амфибия).
- Великобритания: Christopher Ward (C1 Bel Canto).
- Швейцария: добавлен ультралегкий мануфактурный Richard Mille (RM 67-02).
- Все 8 точек стран на 3D-глобусе содержат мануфактуры, модели и интерактивную статистику цен.
- Проверка `uv run horologium check` и 12 тестов `pytest` проходят без единой ошибки.

## Блок 7. Комплексная оптимизация производительности, GPU и плавности скролла (2026-09-25)

**Задачи по требованиям пользователя:**
Оптимизация потребления ресурсов и устранение притормаживаний/лагов, обеспечение ультра-плавного скролла и мгновенной анимации.

**Снижение нагрузки на GPU и оптимизация WebGL:**
- Ограничение pixel ratio в WebGL-рендерерах `WatchStage` и `GlobeScene` с 2.0x до 1.5x (для GTAO composer до 1.25x). На дисплеях Retina и 4K это сократило объём рассчитываемых пикселей на 44%, исключив просадку кадров.
- Оптимизация GTAO (Ground Truth Ambient Occlusion): уменьшено число выборок с 16 до 8, а пуассоновское шумоподавление настроено на 8 сэмплов и 1 кольцо. Нагрузка на шейдеры снизилась более чем в 2 раза при полном сохранении реалистичных теней на металлических узлах.
- Умный жизненный цикл 3D-сцены: в `frontend/js/views/watch.js` добавлен `IntersectionObserver`, который останавливает бесконечный цикл `explodeStage.stop()`, когда блок разборки вне зоны видимости, и запускает его только при появлении. Вне блока разборки нагрузка на видеокарту падает до 0%. При скролле за пределами цикла отрисовка выполняется строго on-demand.
- Скрытие WebGL-холста глобуса (`canvas.style.display = "none"`) при уходе на страницы моделей и брендов, что освобождает видеопамять композитора браузера.
- Исключение лишних reflow в `#updatePins()`: стили меток на карте обновляются только при реальном смещении более 0.2px.

**Плавность скролла и отзывчивость анимаций:**
- В `frontend/js/core/scroll.js` скорректирован коэффициент Lenis `lerp` с 0.085 до 0.12 (добавлен `syncTouch: false`), что убрало «вязкое» залипание прокрутки и восстановило естественный отклик мыши и трекпада.
- Восстановлен демпфер `gsap.ticker.lagSmoothing(500, 33)` вместо отключённого `0`, предотвращая скачки и рывки анимации при микропросадках кадров.
- Уменьшено запаздывание `scrub` в GSAP ScrollTrigger (для героя, разборки и горизонтального таймлайна) с 1.0s / 0.8s до 0.3s. Все элементы теперь точно следуют за жестом скролла без задержек.
- Сокращены тайминги переходов роутера: уход 0.2s–0.24s (был 0.28–0.36s), вход 0.45s–0.55s (был 0.8–0.95s) с кривой `power2.out`, устранив ощущение медлительности интерфейса.

**Оптимизация рендеринга CSS и слоёв композитинга:**
- Удалено ресурсоёмкое размытие `filter: blur(8px)` с переходов картинок `.photo` и фонового слоя `.globe-layer`. Плавное появление теперь работает на аппаратно-ускоренном `opacity` и `will-change`.
- Добавлено свойство `content-visibility: auto; contain-intrinsic-size: ...` для карточек каталога (`.wcard`), строк брендов (`.cbrand`) и блоков усложнений (`.wcomp`). Браузер полностью пропускает расчёт стилей и вёрстки для элементов вне экрана.
- Изолированы в собственные аппаратные GPU-слои (`transform: translateZ(0)`): фоновая SVG-шумовая маска `body::after`, боковая панель страны `.cpanel` и шапка `.nav`, что полностью исключило перерисовку страницы при прокрутке.

## Блок 8. 100% официальные 4K фото, глобальный каталог, сравнение, мультивалютность и Docker (2026-09-25)

**Задачи по требованиям пользователя:**
1. Пункт 1: Замена всех оставшихся вторичных и любительских фото на официальные студийные 4K снимки от производителей и авторизованных ритейлеров (100% покрытие каталога).
2. Пункт 4: Добавление глобального каталога моделей с многофакторной фильтрацией, сравнения часов, мультивалютности и PWA.
3. Добавление production-ready Dockerfile и .dockerignore.

**100% покрытие официальной студийной фотографией:**
- Обработаны и загружены официальные студийные снимки для всех 15 ранее отсутствовавших моделей в трех разрешениях (480, 960, 1600 px) на темном фоне (#07080a): Grand Seiko Snowflake, Casio CasiOak, Citizen Tsuyosa, Breitling Superocean Heritage, Hublot Classic Fusion, Tudor Black Bay GMT, Tudor 1926, Oris Divers 65, H. Moser Endeavour, NOMOS Club Campus, A. Lange Saxonia Thin, A. Lange Odysseus, Sinn 104, Seiko Presage Cocktail Time, Panerai Radiomir.
- Обновлены до официальных промо-рендеров 39 моделей (TAG Heuer Monaco/Carrera/F1/Aquaracer, Zenith Chronomaster/Defy, IWC Portugieser/Mark XX/Ingenieur, JLC Reverso/Polaris/Master, Hublot Big Bang Unico, Blancpain Fifty Fathoms/Villeret, Breguet Classique/Type XX, Tissot PRX, Grand Seiko White Birch, Chopard Alpine Eagle, Longines Spirit Zulu/Master/Pilot, Junghans Max Bill, Rado Captain Cook, Sinn U1, Swatch Sistem51).
- Итог: 103 из 103 моделей (100%) каталога имеют реальные студийные фотографии высокого разрешения. Заглушек нет.
- База данных SQLite пересобрана и полностью синхронизирована (`uv run horologium build-db`, дайджест `28a55d6f746b1819`).

**Глобальный каталог часов (`/watches`):**
- Создан модуль `frontend/js/views/watches.js` с полноценной галереей всех 103 моделей.
- Живой поиск по названию, бренду, коллекции и референсу.
- Мульти-фильтры: по странам (8 стран), по типам механизмов (автоподзавод, ручной завод, кварц, Spring Drive), по ценовым диапазонам (<$1k, $1k-$5k, $5k-$15k, >$15k).
- Сортировка: по умолчанию, цена (по возрастанию / по убыванию), год выпуска, диаметр корпуса.
- Динамический счетчик найденных моделей и плавная анимация карточек.
- Зарегистрированы маршруты в клиентском роутере и серверном обработчике `backend/horologium/web/spa.py` с генерацией SEO-тегов.

**Интерактивное сравнение моделей (`/compare`):**
- Создано хранилище `frontend/js/core/compare.js` с сохранением в `localStorage` (до 4 моделей одновременно).
- На карточки часов и на страницу конкретной модели добавлены кнопки быстрого добавления в сравнение с визуальным индикатором активного состояния.
- Плавающая панель `#compare-float-bar` внизу экрана с отображением количества выбранных моделей, кнопкой перехода к сравнению и быстрой очисткой.
- Детальная интерактивная матрица сравнения в `frontend/js/views/compare.js`: страна происхождения, диаметр корпуса, толщина, водонепроницаемость, тип и калибр механизма, мануфактурность, запас хода, частота полуколебаний, перечень усложнений и актуальная цена.

**Мультивалютность (USD, EUR, RUB, KZT):**
- В `frontend/js/core/format.js` реализована система конвертации и форматирования валют (`CURRENCIES`, `getActiveCurrency()`, `setActiveCurrency()`, `onCurrencyChange()`).
- В шапку сайта добавлен выпадающий селектор валют: USD ($), EUR (€), RUB (₽), KZT (₸).
- Автоматический мгновенный пересчет всех цен на карточках каталога, в шапке модели и в таблице сравнения при смене валюты без перезагрузки страницы.

**PWA и контейнеризация:**
- Создан веб-манифест `frontend/manifest.webmanifest` с поддержкой установки на рабочий стол и мобильные устройства (standalone, темная тема `#07080a`, иконки). В `index.html` добавлены meta-теги для iOS Safari.
- Добавлен `backend/horologium/web/spa.py` статический обработрщик для `/manifest.webmanifest`.
- Создан многоэтапный `Dockerfile` на базе Python 3.12-slim и пакетного менеджера uv: изолированная установка зависимостей, автоматическая предсборка SQLite базы на этапе сборки контейнера и легковесный финальный образ для запуска на порту 8765.
- Добавлен файл `.dockerignore` для исключения лишних файлов (кэши, локальные базы, виртуальные окружения).

**Тестирование и валидация:**
- 13 из 13 тестов в `pytest` успешно пройдены (добавлены тесты роутов каталога, сравнения и веб-манифеста).
- Целостность контента: `uv run horologium check` подтверждает корректность всех 8 стран, 41 бренда, 103 моделей, 101 калибра и 21 усложнения.



